<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= plant.name %></title>
</head>
<body>
    <h1><%= plant.name %></h1>
    <p><%= plant.description %></p>

    <% if (plant.image) { %>
        <img src="/uploads/<%= plant.image %>" alt="<%= plant.name %>" style="max-width: 300px;" />
    <% } else { %>
        <p>No image available.</p>
    <% } %>

    <a href="/plants">Back to Plants</a>
</body>
</html>
